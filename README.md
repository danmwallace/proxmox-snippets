# proxmox-snippets

Some snippets for proxmox

# QEMU Templates

This folder contains a script (intended to be put on the Proxmox server) and 

## User Data (cloud-init config)

This is my `cloud-init` config, which can be used to do early setup of an Ubuntu machine. 